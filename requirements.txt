# Install the local package in editable mode so notebooks and tests use the
# current working tree.
-e .

# Runtime dependencies used by src/optimal_pig.
numpy>=1.24
matplotlib>=3.7
ipykernel>=6
pytest>=7
